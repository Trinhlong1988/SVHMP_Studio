#!/bin/sh
# git-lfs optional (may Admin khong co lfs) — skip neu thieu, KHONG bao loi false exit-2
# (bug 4/7: template git-lfs mac dinh exit 2 khi thieu binary du checkout/worktree
# THANH CONG that -> CMD_BUILD_3 tin exit-2 = fail roi bo cuoc worktree, lam viec
# truc tiep tren shared tree -> conflict that voi G2/CMD_BUILD dang push song song.
# Mirror dung pattern da fix o pre-push, per Mr.Long authorization 2026-07-04.)
if command -v git-lfs >/dev/null 2>&1; then git lfs post-checkout "$@" || exit $?; fi
