#!/bin/sh
# git-lfs optional (may Admin khong co lfs) — skip neu thieu, KHONG bao loi false exit-2
# (bug 4/7, mirror pre-push, per Mr.Long authorization 2026-07-04 — xem post-checkout)
if command -v git-lfs >/dev/null 2>&1; then git lfs post-merge "$@" || exit $?; fi
